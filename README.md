# Student Score Tracker

A simple application for tracking and reporting student scores.

## Features

- Calculate letter grades from numeric scores
- Generate formatted reports
- Export results to CSV
- Summary statistics (average, highest, lowest)

## Usage

```bash
cargo run
```

## Configuration

Edit `config.json` to customize database connection, logging, and feature flags.
